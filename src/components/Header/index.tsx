import { FC } from 'react';
import { Layout } from 'antd';

const { Header: PageHeader } = Layout;

export const Header: FC = () => {
    return (
        <PageHeader className='site-layout-sub-header-background' style={{ padding: 0 }}>
            ehader
        </PageHeader>
    );
};
